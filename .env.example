# OpenBao PKI and ACME Configuration
# Copy this file to .env and customize the values

# OpenBao Connection
OPENBAO_ADDR=http://localhost:8200
OPENBAO_TOKEN=your-root-token-here

# Root CA Configuration
CA_COMMON_NAME="OpenBao Root CA"
CA_TTL="87600h"  # 10 years

# Certificate Role Configuration
# Comma-separated list of allowed domains for the "general" role
ALLOWED_DOMAINS=example.com,localhost

# ACME Configuration
# Use "*" to allow any domain, or specify comma-separated domains
# Supports wildcards like "*.example.com"
ACME_ALLOWED_DOMAINS=*

# Cluster Configuration
# This should be the publicly accessible URL of your OpenBao instance
# For local development: http://localhost:8200
# For production: https://openbao.example.com
CLUSTER_PATH=http://localhost:8200

# CA Import Configuration (optional - for importing existing CA certificates)
# Uncomment and set these if you want to import an existing CA instead of generating a new one
# CA_CERT_PATH=./path/to/ca.crt
# CA_KEY_PATH=./path/to/ca.key
# Or use a bundle file (certificate + key in one file):
# CA_BUNDLE_PATH=./path/to/ca-bundle.pem
# ISSUER_NAME=imported-ca

# Docker Configuration
VAULT_ADDR=http://127.0.0.1:8200
SKIP_SETCAP=true
